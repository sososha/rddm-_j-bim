CREATE TABLE IF NOT EXISTS projects (`n    id SERIAL PRIMARY KEY,`n    name VARCHAR(255) NOT NULL,`n    description TEXT,`n    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,`n    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`n);`n`nCREATE TABLE IF NOT EXISTS drawings (`n    id SERIAL PRIMARY KEY,`n    project_id INTEGER REFERENCES projects(id),`n    name VARCHAR(255) NOT NULL,`n    data JSONB NOT NULL,`n    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,`n    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`n);
